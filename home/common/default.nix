# Common Home Manager modules shared across all platforms
# Platform-agnostic settings: git, gpg, zsh core, common packages
# To be populated in Task 9 (Home Common Migration)

{ config, pkgs, inputs, ... }:
{
  # Placeholder for common HM modules
}
