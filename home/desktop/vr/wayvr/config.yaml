# Niri and WayVR 26.2.1 cannot negotiate a video format through PipeWire CPU;
# PipeWire GPU reaches Streaming with BGRx DMA-BUF on NVIDIA.
capture_method: pipewire

space_drag_multiplier: 1.0
space_drag_unlocked: true
space_rotate_unlocked: false

screen_render_down: false
