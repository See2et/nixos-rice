# Niri's screencopy path can freeze; use the PipeWire portal fallback for
# desktop mirrors. The user still chooses the monitor in the portal picker.
capture_method: pw-fallback

space_drag_multiplier: 1.0
space_drag_unlocked: true
space_rotate_unlocked: false

screen_render_down: false
